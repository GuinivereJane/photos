<p><%= @photo.title %>, <%= @photo.artist %></p>
<%= image_tag @photo.url %>
<%= link_to "Back to Photos", photos_url %> |
 <%= link_to "Edit", edit_photo_path(@photo) %> |
  <%= link_to "Delete", @picture, method: :delete,
  data: {confirm: "Are you sure you want to delete this picture?"}  %>
